const runLengthEncoding = (str) => {
  let result =  [] 
   let count = 1
  for (i=0; i<str.length; i++){
    if (str[i] !== str[i+1]) {
      result.push([count, str[i]]) 
      count = 1
    } else {
      count ++
    }
  }
  return result
}
