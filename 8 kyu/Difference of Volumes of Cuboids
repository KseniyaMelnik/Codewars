function findDifference(a, b) {
  const aVolume = a.reduce((previousValue, currentValue) => previousValue * currentValue) 
  const bVolume = b.reduce((previousValue, currentValue) => previousValue * currentValue)
  return Math.abs(aVolume-bVolume)
}
