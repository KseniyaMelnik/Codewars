const solve = (arr) => {  
return arr.map((el)=>{
            let sum = 0
            for(let i = 0; i<el.length; i++){
                  (i+65 === el.toUpperCase().charCodeAt(i))? sum++ : sum                  
             } 
            return sum
            })
};
